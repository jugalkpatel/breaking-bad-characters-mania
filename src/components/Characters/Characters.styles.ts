import styled from "styled-components";

const Loader = styled.section`
  margin: 0 auto;
  display: flex;
  justify-content: center;
  align-items: center;
  grid-column: 1 / -1;
  height: 70vh;
`;

export { Loader };
